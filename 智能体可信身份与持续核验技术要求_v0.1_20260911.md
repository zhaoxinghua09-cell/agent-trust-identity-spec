# 智能体可信身份与持续核验技术要求

**Technical Requirements for Trusted Agent Identity and Continuous Verification**

版本：v0.1（技术草案）
日期：2026-09-11
状态：公开草案 / Public Draft
许可：MIT
语言：中文（英文版另出）

---

## 前言

本文件给出智能体身份在**运行期**可验证性的技术要求。

本文件的出发点是：既往的身份方案多在**签发时刻**判定可信，此后不再复算；而智能体的行为发生在签发之后，且其记忆、权限、行为边界均可随时间变化。**以"签发时刻可信"推定"运行期持续可信"，是不可靠的。**

本文件因此主张：**持续核验应作为默认行为，而非可选的加强项。**

本文件不含任何特定厂商、平台或组织的立场，不构成标准提案，不构成合规意见。

---

## 目次

1. 范围
2. 规范性引用文件
3. 术语和定义
4. 智能体身份参考模型
5. 四环节技术要求（核心章）
6. 证据与凭证结构
7. 验证流程
8. 符合性评估方法
9. 特定领域实施要求（医疗器械）
10. 实施参考
附录 A（资料性）判定规则码汇总
附录 B（资料性）符合性自评清单
附录 C（资料性）与他人工作的关系

---

## 1 范围

### 1.1 本文件规定什么

本文件规定智能体（Agent）在**身份运行期**的可验证性要求，覆盖四个环节：

- a) 身份注册（Identity Registration）
- b) 授权与意图传递（Authorization and Intent Transfer）
- c) 运行状态持续核验（Continuous Runtime Verification）
- d) 审计追溯（Audit Traceability）

对每个环节，本文件给出**最小可验证要求（Minimum Verifiable Requirement，MVR）**——即一套可照着实现、可独立检验的最低要求集。

### 1.2 本文件不规定什么

本文件**不规定**：

- a) 智能体的智能水平、模型架构、训练方法；
- b) 业务功能的实现方式；
- c) 身份签发机构的资质认定；
- d) 任何具体产品的接口细节（接口为资料性参考，见第 10 章）。

### 1.3 适用对象

本文件适用于：

- a) 需要为智能体建立可核验身份的开发方与运营方；
- b) 需要在无平台依赖、无网络连接的条件下核验智能体身份的场景；
- c) 需要对智能体行为做独立第三方复核的场景；
- d) 需要将智能体身份证据纳入既有质量或合规体系的组织。

### 1.4 适用边界

本文件的技术要求以**本地可独立复算**为设计前提。对于必须依赖外部服务方可完成的核验动作，本文件不提供要求，但允许实现方在本文件要求之外自行扩展。

---

## 2 规范性引用文件

下列文件中的内容通过文中的规范性引用而构成本文件必不可少的条款。

| 编号 | 文件 | 引用点 |
|---|---|---|
| [1] | W3C Decentralized Identifiers (DID) v1.0 | 第 4.2 条身份标识形式 |
| [2] | W3C Verifiable Credentials Data Model v2.0 | 第 6.2 条凭证结构 |
| [3] | ISO/IEC 42001:2023 人工智能管理体系 | 第 8 章符合性评估 |
| [4] | ISO/IEC 27001 信息安全管理体系 | 第 7 章证据完整性 |
| [5] | NIST FIPS 180-4 SHA-256 安全散列标准 | 第 6.3 条指纹算法 |
| [6] | NIST SP 800-57 密钥管理建议 | 第 6.4 条密钥与中止权 |
| [7] | GB/T 1.1 标准化文件的结构和起草规则 | 本文件体例 |

> 注：引用文件以其发布机构的最新有效版本为准。本文件引用的版本为编制时（2026-09）的有效版本。

---

## 3 术语和定义

下列术语和定义适用于本文件。

### 3.1 智能体 agent

能够自主执行动作的软件实体。其动作可作用于外部对象，并可随时间演化。

### 3.2 身份锚 identity anchor

用于确定智能体"是谁"的、一经签发不可变的一组属性。至少包含身份标识与适用声明。

### 3.3 记忆锚 memory anchor

用于确定智能体"基于什么"的、可回溯的一组指纹。至少包含训练或知识来源的指纹与验证摘要。

### 3.4 行为锚 behavior anchor

用于确定智能体"被允许做什么、由谁中止"的一组约束。至少包含不伤害边界、人机边界、中止机制。

### 3.5 验证票 verification ticket

用于第三方独立核验的凭证结构，与上述三锚同源绑定。

### 3.6 三锚一票 three anchors and one ticket

由身份锚、记忆锚、行为锚与验证票构成的、同源绑定的身份凭据整体。

### 3.7 指纹 fingerprint

由确定性算法对给定内容计算得到的固定长度摘要，用于检出内容变更。本文件采用 SHA-256。

### 3.8 最小可验证要求 minimum verifiable requirement, MVR

一组最低限度的、可独立检验的要求。满足 MVR 即可被判定为该环节符合；不满足则判定为不符合。

### 3.9 持续核验 continuous verification

在每次放行前重新计算并比对指纹，而非仅在签发时核验一次的行为。

### 3.10 审计链 audit chain

由逐条记录前后哈希关联形成的、只增不改的记录序列。

### 3.11 台账对账 registry reconciliation

将待验凭证与签发台账比对，以发现伪造或已中止凭证的行为。

### 3.12 拒验 refuse to verify

在核验条件不满足时，判定为不可信并拒绝放行。**本文件规定：拒验为默认结果，放行为需条件的例外。**

---

## 4 智能体身份参考模型

### 4.1 总体结构

智能体身份由**三锚一票**构成，四者**同源绑定**——即任一锚的内容变更均导致验证票指纹失效。

```
              ┌──────────────────────────────────────┐
              │        智能体身份 (Agent Identity)      │
              └──────────────────────────────────────┘
                              │
      ┌───────────────┬───────┴───────┬───────────────┐
      ▼               ▼               ▼               ▼
┌───────────┐   ┌───────────┐   ┌───────────┐   ┌───────────┐
│  身份锚    │   │  记忆锚    │   │  行为锚    │   │  验证票    │
│ identity  │   │  memory   │   │ behavior  │   │  ticket   │
├───────────┤   ├───────────┤   ├───────────┤   ├───────────┤
│ 身份标识   │   │ 数据指纹   │   │ 不伤害边界 │   │ 标识符     │
│ 适用声明   │   │ 验证摘要   │   │ 人机边界   │   │ 凭证类型   │
│ 责任主体   │   │ 来源可溯   │   │ 中止机制   │   │ 签发与指纹 │
└───────────┘   └───────────┘   └───────────┘   └───────────┘
      └───────────────┴───────┬───────┴───────────────┘
                              ▼
                    SHA-256 指纹绑定（同源不可分离）
```

### 4.2 身份标识

智能体身份标识应满足：

- a) 全局唯一；
- b) 一经签发不可变更（变更须以新建标识 + 变更记录方式处理）；
- c) 可采用 DID 形式（如 `did:web:<domain>:agent:<id>`）或与之等价的唯一标识符。

**MVR-4.2**：标识符不得因内容更新而复用；不得存在两个不同身份共享同一标识符的情形。

### 4.3 生命周期状态

智能体身份应具有明确的生命周期状态，且状态迁移须留痕。

| 状态 | 含义 |
|---|---|
| `in_development` | 开发中，未对外提供 |
| `under_review` | 评审中 |
| `approved` | 已批准，未运行 |
| `in_market` | 运行中 |
| `suspended` | 已中止（中止权行使后） |
| `retired` | 已退役 |

**MVR-4.3**：状态迁移须记录发起者、时间戳与前序状态；缺失任一项 → 该次迁移无效。

### 4.4 锚的齐备性

**MVR-4.4**：三锚与验证票**必须齐备**。缺任一锚或缺验证票者，不得进入注册环节，不得进入放行环节。

---

## 5 四环节技术要求（核心章）

> **本章为全文核心。** 以下逐环节给出最小可验证要求（MVR）。每个 MVR 均标注**判定规则码**（见附录 A），供机器检验与人工复核使用。

### 5.1 环节一：身份注册

#### 5.1.1 要求

| 编号 | 要求 | 判定规则码 |
|---|---|---|
| MVR-5.1-a | 注册须提交完整三锚一票；缺任一锚或缺验证票者，**拒绝注册** | `REG-001` |
| MVR-5.1-b | 身份标识一经签发不可变；变更须新建标识并保留原标识的变更记录 | `REG-002` |
| MVR-5.1-c | 验证票指纹须在注册时计算并固化，作为后续持续核验的基准 | `REG-003` |
| MVR-5.1-d | 注册须记录责任主体（签发者）；无责任主体者不得注册 | `REG-004` |
| MVR-5.1-e | 注册结果（通过/拒绝）须写入台账，**拒绝亦须留痕** | `REG-005` |

#### 5.1.2 说明

MVR-5.1-a 是本章的基础条款。其作用在于：**把"齐备"从倡导性要求变为门禁条件。** 在传统做法中，缺失的锚常被允许后续补齐；本文件规定，齐备性是注册的前置条件，不齐备即不成立。

MVR-5.1-e 强调**拒绝留痕**。拒绝记录本身是治理证据：它证明系统确实执行了判定，而非默认放行。

### 5.2 环节二：授权与意图传递

#### 5.2.1 要求

| 编号 | 要求 | 判定规则码 |
|---|---|---|
| MVR-5.2-a | 授权须**显式声明允许范围**（动作白名单）；未列入白名单的动作视为不允许 | `AUT-001` |
| MVR-5.2-b | 授权须**显式声明有效期**；超出有效期者视为失效 | `AUT-002` |
| MVR-5.2-c | 授权凭证的指纹须与三锚同源绑定；任一锚变更导致授权失效 | `AUT-003` |
| MVR-5.2-d | 意图传递须留下**可回溯凭证**；无凭证的意图传递视为未传递 | `AUT-004` |
| MVR-5.2-e | 越权动作须被门禁**拦截**，并产生拦截记录 | `AUT-005` |

#### 5.2.2 说明

MVR-5.2-a 采用**白名单**语义而非黑名单语义。原因是：智能体可执行的动作空间是开放的，黑名单无法穷尽。未列即不允许，是唯一可判定且可维持的一致语义。

MVR-5.2-d 把"意图"从隐含状态变为显式凭证。这使"智能体当初被要求做什么"成为可核验事实，而非事后追述。

### 5.3 环节三：运行状态持续核验

> **本节是全文主张最强的部分。**

#### 5.3.1 要求

| 编号 | 要求 | 判定规则码 |
|---|---|---|
| MVR-5.3-a | **不接受"注册时核验通过即持续可信"**；每次放行前须重新计算指纹 | `CVF-001` |
| MVR-5.3-b | 重算指纹与注册基准不一致者，**一律拒验**，不区分场景 | `CVF-002` |
| MVR-5.3-c | 拒验须记录比对双方摘要与时间戳 | `CVF-003` |
| MVR-5.3-d | 不得提供"跳过核验"的降级开关；如需放宽，须以新建授权实现 | `CVF-004` |
| MVR-5.3-e | 核验不依赖外部服务即可完成；离线环境下的核验结果与联网环境一致 | `CVF-005` |

#### 5.3.2 说明（技术贡献点）

**本文件的核心技术主张为 MVR-5.3-a 与 MVR-5.3-b 的组合**：

> **缺锚或缺指纹者，一律拒验；不区分场景，不允许降级。**

该主张的技术意义在于，它把"持续核验"从**可选的加强项**变为**默认行为**：

- 传统做法：默认放行，风险场景追加核验 → 核验是例外
- **本文件做法：默认拒验，条件齐备方可放行 → 放行是例外**

MVR-5.3-d 是维持该默认行为不被侵蚀的必要条款。若允许存在"跳过核验"的开关，则该开关在实践压力下必然被使用，默认行为随即退化为可选行为。**因此本文件禁止降级开关，只允许通过新建授权来调整，使每次调整均留下记录。**

MVR-5.3-e 使该要求在无网络、无平台依赖的环境下同样成立。

### 5.4 环节四：审计追溯

#### 5.4.1 要求

| 编号 | 要求 | 判定规则码 |
|---|---|---|
| MVR-5.4-a | 全链路留痕，记录序列**只增不改**；任何修改须以追加方式表达 | `AUD-001` |
| MVR-5.4-b | 记录之间以前后哈希关联，形成审计链；链断即判为可疑 | `AUD-002` |
| MVR-5.4-c | **第三方可独立复算**，不依赖记录生成方提供的额外信息 | `AUD-003` |
| MVR-5.4-d | 台账对账须能发现**伪造**（无签发记录）与**已中止**（中止记录存在）两类情形 | `AUD-004` |
| MVR-5.4-e | 每类证据均须可定位到具体文件及其指纹 | `AUD-005` |

#### 5.4.2 说明

MVR-5.4-c 是"可独立复算"要求。其含义是：第三方仅凭记录本身，即可重算出相同的哈希，从而验证记录未被篡改。**这使审计不再依赖对记录生成方的信任。**

MVR-5.4-d 把对账从"存在性检查"升级为"双向量检查"：既查伪造（本条记录不该存在），也查中止（本条记录已失效）。

### 5.5 四环节与 MVR 对照总表

| 环节 | MVR 数量 | 判定规则码前缀 | 核验动作 | 不满足时结果 |
|---|---|---|---|---|
| ① 身份注册 | 5 | `REG-` | 检查三锚一票齐备性 | 拒绝注册 |
| ② 授权与意图传递 | 5 | `AUT-` | 检查白名单、有效期、凭证 | 门禁拦截 |
| ③ 运行状态持续核验 | 5 | `CVF-` | 重算指纹并与基准比对 | 一律拒验 |
| ④ 审计追溯 | 5 | `AUD-` | 链式校验 + 台账对账 + 独立复算 | 判定可疑 |

> **合计 20 条 MVR。** 第 8 章给出对应的自评方法。

---

## 6 证据与凭证结构

### 6.1 证据分类

本文件要求智能体身份的证据分为六类：

| 类别 | 内容 |
|---|---|
| `01-identity` | 身份证据：凭据本体、登记文件、身份标识 |
| `02-data` | 数据证据：数据来源与指纹 |
| `03-validation` | 验证证据：评价报告、指标、文献 |
| `04-behavior` | 行为边界证据：不伤害边界、人机边界、中止规程 |
| `05-change` | 变更证据：变更记录与评估结论 |
| `06-issuance` | 签发证据：门禁结论、签发者、时间戳 |

**MVR-6.1**：六类证据**必须齐备**方可进入放行环节；缺任一类 → 拒验。

> 注：此条与 MVR-5.3-a/b 呼应——六类证据的齐备性是持续核验的检查项之一。

### 6.2 凭证结构

验证票应采用以下结构（字段名为资料性，语义为规范性）：

```json
{
  "schema_version": "1.0",
  "agent_id": "<全局唯一标识符，一经签发不可变>",
  "identity_anchor": {},
  "memory_anchor": {},
  "behavior_anchor": {},
  "verification_ticket": {
    "issuer": "<责任主体>",
    "issuance_date": "<ISO 8601>",
    "vc_type": "VerifiableCredential",
    "credential_fingerprint": "sha256:<...>"
  },
  "change_log": [],
  "lifecycle_status": "in_development"
}
```

**MVR-6.2**：`credential_fingerprint` 须覆盖三锚与验证票的全部字段；任一字段变更须导致指纹变化。

### 6.3 指纹算法

- a) 采用 SHA-256；
- b) 计算前须对内容做**规范化序列化**（键排序、固定编码、统一空值表示）；
- c) 摘要以 `sha256:<十六进制>` 形式表示。

**MVR-6.3**：实现方须公开其规范化规则；未公开者，第三方无法复算，视为不满足 MVR-5.4-c。

### 6.4 链式关联

变更记录与证据记录均采用链式关联：

```
block[n].prev      = block[n-1].block_hash
block[n].block_hash = SHA256(block[n] 除 block_hash 外的全部字段)
block[0].prev      = "genesis"
```

**MVR-6.4**：链头须固化并可在核验时复算；链头不一致 → 判为可疑。

### 6.5 中止机制

- a) 行为锚须包含中止机制（中断/回滚）的存在性声明与责任主体；
- b) 中止一经执行，须写入台账，并导致后续对该身份的一切核验**一律拒验**。

**MVR-6.5**：`kill_switch.exists = false` 的身份，不得进入放行环节。

---

## 7 验证流程

### 7.1 三重验真

核验一个智能体身份，须依次完成三重校验：

| 序 | 校验项 | 动作 | 不通过时 |
|---|---|---|---|
| 1 | **结构完整** | 检查必需字段是否齐备 | 拒验（`CVF-004` 不适用） |
| 2 | **指纹防篡改** | 按公开规则重算指纹，与凭证内指纹比对 | 拒验（篡改或非同源签发） |
| 3 | **台账对账** | 查签发记录存在且指纹一致 | 拒验（伪造或已中止） |

**MVR-7.1**：三重校验**全部通过**方可判定为可信；任一不通过 → 拒验。**不允许"部分通过即放行"。**

### 7.2 持续核验的触发点

持续核验应在以下时点触发：

- a) 每次动作放行前；
- b) 每次权限使用前；
- c) 每次跨系统交互前；
- d) 定期（建议不低于每日一次）。

**MVR-7.2**：a)、b) 两项为强制触发点；缺失任一者不满足 MVR-5.3-a。

### 7.3 核验输出

核验须输出**可记录的判定结果**，包含：

- a) 总体判定（通过 / 拒验）；
- b) 各校验项结果；
- c) 不通过时的规则码；
- d) 时间戳与核验者标识。

**MVR-7.3**：核验输出须纳入审计链（见 5.4）。

### 7.4 拒验的语义

在本文件中，**拒验不是异常，而是正常输出之一**。实现方不得将拒验视为系统故障，不得因拒验频繁而自动放宽判定条件。

**MVR-7.4**：任何以"降低拒验率"为目标的自动调参行为，均视为违反 MVR-5.3-d。

---

## 8 符合性评估方法

### 8.1 评估方式

符合性评估采用**自评 + 独立复核**两种方式：

| 方式 | 主体 | 依据 |
|---|---|---|
| 自评 | 实现方 | 附录 B 自评清单 |
| 独立复核 | 第三方 | 按第 7.1 条三重验真独立执行 |

### 8.2 判定结论

每个 MVR 的判定结论为**符合 / 不符合**二元值，不接受中间态。

- a) 全部 20 条 MVR 符合 → 判定为**符合本文件**；
- b) 存在任一不符合 → 判定为**不符合本文件**，并列出不符合项及其规则码。

**MVR-8.2**：判定结论须引用具体规则码，不得以"基本符合""部分符合"表述。

### 8.3 评估记录

评估结果须形成记录，包含：

- a) 被评估身份标识；
- b) 逐条 MVR 判定结果；
- c) 整体结论；
- d) 评估者与时间戳；
- e) 支撑证据的定位信息与指纹。

### 8.4 与既有管理体系的关系

本文件的符合性评估结果可作为输入，供组织在 ISO/IEC 42001、ISO 13485 等体系内使用。**本文件不替代上述体系的任何要求，亦不构成对其的符合性声明。**

---

## 9 特定领域实施要求（医疗器械）

> **本章为可选章。** 未采用本章的组织，不影响其对第 1 至 8 章的符合性。
> 本章给出医疗器械领域（含人工智能医疗软件）的实施要求，作为本文件在受监管场景下的应用示例。

### 9.1 适用的额外要求

| 编号 | 要求 | 对应通用 MVR |
|---|---|---|
| DM-9.1-a | 身份锚须包含**风险分类**（按目的市场分类规则）与**上市状态** | MVR-5.1-a |
| DM-9.1-b | 记忆锚须包含**数据集指纹**（含人群分布、样本量、标注方式） | MVR-6.2 |
| DM-9.1-c | 行为锚须包含**人机边界**（如"须经具备资质人员复核方可生效"） | MVR-5.2-a |
| DM-9.1-d | 行为锚须包含**中止机制**及其责任主体 | MVR-6.5 |
| DM-9.1-e | 变更记录须标注是否落在**预定变更控制**范围内 | MVR-5.4-a |
| DM-9.1-f | 运行状态持续核验须覆盖**模型版本变更**与**运行环境变更** | MVR-5.3-a |

### 9.2 医疗场景下的持续核验

医疗场景的特殊性在于：**模型变更（含数据漂移引发的性能变化）可能影响安全有效性。** 因此：

- a) 每次模型版本变更须触发重新核验；
- b) 数据漂移监测结果超出预设阈值时，须触发重新核验；
- c) 上述触发**不得被关闭**（对应 MVR-5.3-d）。

**MVR-9.2**：a)、b) 两项触发条件须在行为锚中显式声明。

### 9.3 免责声明

本章为技术实施参考，**不构成任何监管符合性意见**。涉及具体产品的合规判定，须以主管机关现行有效法规与指南为准，并由具备资质的主体作出。

术语与分类口径随法规更新而变化，实际使用时应核对最新有效版本。

---

## 10 实施参考

> **本章为资料性内容，不构成要求。**

### 10.1 参考实现映射

| 本文件章节 | 可参照的实现构件 |
|---|---|
| 第 4 章参考模型 | `algorithm-passport-schema-v1.0.json`（三锚一票结构，可直接复用） |
| 第 5.1 条身份注册 | 闭环认证工具的 `register` 命令（签发身份凭据 + 指纹固化） |
| 第 5.2 条授权传递 | 登机牌构件（身份 + 证据指纹 + 动作白名单 + 有效期） |
| 第 5.2 条权限边界 | 权限策略生成器（策略即代码，白名单 + 四道门禁） |
| 第 5.3 条持续核验 | 每次放行前重算指纹的核验流程 |
| 第 5.4 条审计追溯 | 行为留痕审计构件（JSONL 动作日志 → 账本 → 越界项标出） |
| 第 6 章证据结构 | 六类证据目录 + 链式哈希 |
| 第 7.1 条三重验真 | 徽章验真构件（结构 / 指纹 / 台账三重） |
| 第 8 章符合性 | 合规自评器（rubric 评分卡） |

### 10.2 最小实现路径

一个满足本文件的最小实现包含：

- a) 一个身份凭据结构（第 6.2 条）；
- b) 一个签发动作（含齐备性检查，第 5.1 条）；
- c) 一个核验动作（含三重校验，第 7.1 条）；
- d) 一个拒验路径（含留痕，第 7.3 条）；
- e) 一个审计链（第 6.4 条）。

上述五项均可在**零外部依赖、纯本地**条件下实现。

### 10.3 离线可行性

本文件全部强制要求均可离线完成。验证票与指纹均可本地计算；台账对账可基于本地文件完成；审计链校验可本地复算。

**这也是本文件与依赖平台服务方案的主要区别所在**——但不构成本文件对任何方案的优劣评价。

---

## 附录 A（资料性）判定规则码汇总

| 规则码 | 含义 | 对应条款 |
|---|---|---|
| `REG-001` | 三锚一票不齐备 | MVR-5.1-a |
| `REG-002` | 身份标识被复用或发生原地修改 | MVR-5.1-b |
| `REG-003` | 注册时未固化验证票指纹 | MVR-5.1-c |
| `REG-004` | 无责任主体 | MVR-5.1-d |
| `REG-005` | 注册结果未留痕（含拒绝未留痕） | MVR-5.1-e |
| `AUT-001` | 授权未声明动作白名单 | MVR-5.2-a |
| `AUT-002` | 授权未声明有效期或已过期 | MVR-5.2-b |
| `AUT-003` | 授权指纹未与三锚绑定 | MVR-5.2-c |
| `AUT-004` | 意图传递无可回溯凭证 | MVR-5.2-d |
| `AUT-005` | 越权动作未拦截或无拦截记录 | MVR-5.2-e |
| `CVF-001` | 未在放行前重算指纹 | MVR-5.3-a |
| `CVF-002` | 指纹不一致但未拒验 | MVR-5.3-b |
| `CVF-003` | 拒验未记录比对摘要 | MVR-5.3-c |
| `CVF-004` | 存在跳过核验的降级开关 | MVR-5.3-d |
| `CVF-005` | 核验依赖外部服务，离线不可用 | MVR-5.3-e |
| `AUD-001` | 记录序列存在原地修改 | MVR-5.4-a |
| `AUD-002` | 审计链断裂 | MVR-5.4-b |
| `AUD-003` | 第三方无法独立复算 | MVR-5.4-c |
| `AUD-004` | 台账对账未覆盖伪造与中止两类 | MVR-5.4-d |
| `AUD-005` | 证据无法定位到文件与指纹 | MVR-5.4-e |

---

## 附录 B（资料性）符合性自评清单

> 用法：逐条作答。凡答"否"者，即为不符合项，记录对应规则码。

### 环节一 · 身份注册

- [ ] 1. 是否每次注册均检查三锚一票齐备性？（否则 `REG-001`）
- [ ] 2. 身份标识是否一经签发即不可变？（否则 `REG-002`）
- [ ] 3. 注册时是否计算并固化验证票指纹？（否则 `REG-003`）
- [ ] 4. 是否记录责任主体？（否则 `REG-004`）
- [ ] 5. 拒绝注册是否也写入台账？（否则 `REG-005`）

### 环节二 · 授权与意图传递

- [ ] 6. 授权是否显式声明动作白名单？（否则 `AUT-001`）
- [ ] 7. 授权是否显式声明有效期？（否则 `AUT-002`）
- [ ] 8. 授权指纹是否与三锚同源绑定？（否则 `AUT-003`）
- [ ] 9. 意图传递是否留下可回溯凭证？（否则 `AUT-004`）
- [ ] 10. 越权动作是否被拦截并记录？（否则 `AUT-005`）

### 环节三 · 运行状态持续核验

- [ ] 11. 是否每次放行前重算指纹？（否则 `CVF-001`）
- [ ] 12. 指纹不一致时是否一律拒验（不区分场景）？（否则 `CVF-002`）
- [ ] 13. 拒验是否记录双方摘要与时间戳？（否则 `CVF-003`）
- [ ] 14. 是否不存在"跳过核验"的降级开关？（否则 `CVF-004`）
- [ ] 15. 核验是否可离线完成且结果一致？（否则 `CVF-005`）

### 环节四 · 审计追溯

- [ ] 16. 记录序列是否只增不改？（否则 `AUD-001`）
- [ ] 17. 记录之间是否以前后哈希关联？（否则 `AUD-002`）
- [ ] 18. 第三方是否可独立复算？（否则 `AUD-003`）
- [ ] 19. 台账对账是否覆盖伪造与中止两类？（否则 `AUD-004`）
- [ ] 20. 证据是否可定位到文件与指纹？（否则 `AUD-005`）

**判定**：全部答"是" → 符合本文件；否则列出不符合项规则码。

---

## 附录 C（资料性）与他人工作的关系

> **本附录为说明性内容。** 本文件不评价任何其他方案，仅说明适用场景的差异。

| 维度 | 依赖平台服务的方案 | 本文件关注的场景 |
|---|---|---|
| 信任主体 | 机构对机构 | 个人对个人 / 小微主体 |
| 依赖 | 平台 + 后端 + 互认机制 | 零外部依赖、可离线 |
| 放行依据 | 平台签发与核验 | 本地可独立复算的指纹核对 |
| 数据位置 | 云端注册与追溯 | 本机自持 |
| 互认条件 | 需跨域互认安排 | 算法一致即可互验 |

**本文件的定位**：适用于无平台依赖、无网络连接、需要对身份做独立复核的场景。与依赖平台服务的方案**互补**，覆盖长尾与离线场景。

本文件对上述方案不作优劣评价，亦不主张替代关系。

---

## 版权与许可

© XLGD · SynomosAI Governance Line

本文件采用 MIT 许可发布。

**免责声明**：本文件为技术草案，不构成标准提案，不构成合规意见，不构成对任何机构、组织或产品的立场。文中引用文件以其发布机构最新有效版本为准（截至 2026-09-11）。

本文件不含医疗诊断、治疗或疗效声明；第 9 章为技术实施参考，不构成监管符合性意见。

---

**版本记录**

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-11 | 首次公开草案（八章 + 医械专项章 + 三附录） |
